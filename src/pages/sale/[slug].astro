---
/**
 * 販売品目詳細ページ
 * SSG: getStaticPathsで全品目のパスを生成
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getSaleItemList, getSaleItemBySlug } from '../../lib/microcms';

export async function getStaticPaths() {
  const { contents } = await getSaleItemList({ limit: 100 });
  return contents.map((item) => ({
    params: { slug: item.slug || item.id },
  }));
}

const { slug } = Astro.params;
const item = await getSaleItemBySlug(slug as string);

const imgSrc = item.image?.url;
---

<BaseLayout
  title={`${item.name} | 福祉用具販売 | 株式会社後藤 福祉事業部 つなぐ`}
  description={item.desc.replace(/<[^>]*>/g, '').slice(0, 120)}
>
  <main>
    <div class="page-header">
      <div class="container">
        <a href="/sale" class="back-link">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
            <path d="M15.41 16.59L10.83 12l4.58-4.59L14 6l-6 6 6 6z" />
          </svg>
          販売品目一覧へ
        </a>
      </div>
    </div>

    <section class="section">
      <div class="container detail-container">
        {imgSrc && (
          <div class="detail-img">
            <img src={imgSrc} alt={item.name} />
          </div>
        )}
        <div class="detail-body">
          <span class="section-label">Purchase</span>
          <h1 class="detail-title">{item.name}</h1>
          <div class="detail-desc" set:html={item.desc} />
          {item.content && (
            <div class="detail-content" set:html={item.content} />
          )}
          <div class="detail-insurance">
            <h2>介護保険での購入について</h2>
            <p>
              介護保険を利用して購入でき、年間<strong>10万円まで</strong>が支給対象（自己負担1〜3割）。
            </p>
            <p class="insurance-note">
              ※ 指定事業者からの購入が条件です。"つなぐ"は指定福祉用具販売事業者です。
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="detail-cta">
      <div class="container">
        <h2>{item.name}のご相談は<br />お気軽にお問い合わせください</h2>
        <p>どの用具が対象になるか、お見積りなど何でもお問い合わせください。</p>
        <div class="cta-actions">
          <a href="/contact" class="btn-cta-white">
            無料相談・お問い合わせ
            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M8.59 16.59L13.17 12 8.59 7.41 10 6l6 6-6 6z" />
            </svg>
          </a>
          <a href="/sale" class="btn-cta-outline">
            販売品目一覧へ
          </a>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  .page-header {
    padding: 96px 32px 24px;
    background: var(--color-bg2);
    border-bottom: 1px solid var(--color-border);
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 13px;
    color: var(--color-text-lt);
    transition: var(--transition-base);
  }

  .back-link:hover { color: var(--color-primary); }

  .detail-container {
    display: grid;
    grid-template-columns: 320px 1fr;
    gap: 48px;
    align-items: start;
  }

  .detail-container:not(:has(.detail-img)) {
    grid-template-columns: 1fr;
  }

  .detail-img {
    background: white;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .detail-img img {
    width: 100%;
    display: block;
  }

  .detail-title {
    font-family: var(--font-serif-jp);
    font-size: clamp(24px, 3vw, 32px);
    font-weight: 400;
    color: var(--color-primary);
    margin-bottom: 16px;
    line-height: 1.4;
  }

  .detail-desc {
    font-size: 15px;
    color: var(--color-text-mid);
    line-height: 1.9;
    margin-bottom: 32px;
  }

  .detail-desc :global(p) { margin: 0; }

  .detail-content {
    font-size: 15px;
    color: var(--color-text-mid);
    line-height: 2.0;
    margin-bottom: 32px;
  }

  .detail-content :global(p) { margin-bottom: 1.5em; }

  .detail-content :global(h2) {
    font-family: var(--font-serif-jp);
    font-size: 20px;
    font-weight: 400;
    color: var(--color-primary);
    margin: 2em 0 1em;
    padding-left: 16px;
    border-left: 3px solid var(--color-accent);
  }

  .detail-content :global(h3) {
    font-size: 17px;
    font-weight: 500;
    color: var(--color-primary);
    margin: 1.5em 0 0.8em;
  }

  .detail-content :global(ul),
  .detail-content :global(ol) {
    padding-left: 1.5em;
    margin-bottom: 1.5em;
  }

  .detail-content :global(li) { margin-bottom: 0.5em; }

  .detail-content :global(img) {
    max-width: 100%;
    height: auto;
    border-radius: var(--radius-md);
    margin: 1.5em 0;
  }

  .detail-content :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 1.5em 0;
  }

  .detail-content :global(th),
  .detail-content :global(td) {
    padding: 12px 16px;
    font-size: 14px;
    border: 1px solid var(--color-border);
    text-align: left;
  }

  .detail-content :global(th) {
    background: var(--color-bg2);
    color: var(--color-primary);
    font-weight: 500;
  }

  .detail-insurance {
    background: var(--color-accent-lt);
    border-left: 4px solid var(--color-accent);
    border-radius: var(--radius-md);
    padding: 24px 28px;
  }

  .detail-insurance h2 {
    font-family: var(--font-serif-jp);
    font-size: 16px;
    font-weight: 400;
    color: var(--color-primary);
    margin-bottom: 8px;
  }

  .detail-insurance p {
    font-size: 13px;
    color: var(--color-text-mid);
    line-height: 1.8;
    margin-bottom: 4px;
  }

  .insurance-note {
    font-size: 12px;
    color: var(--color-text-lt);
    margin-bottom: 0;
  }

  /* === CTA === */
  .detail-cta {
    background: linear-gradient(135deg, var(--color-primary) 0%, #001850 100%);
    padding: 80px 32px;
    text-align: center;
  }

  .detail-cta h2 {
    font-family: var(--font-serif-jp);
    font-size: clamp(20px, 2.5vw, 28px);
    font-weight: 400;
    color: white;
    margin-bottom: 12px;
    line-height: 1.6;
  }

  .detail-cta p {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 32px;
  }

  .cta-actions {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-cta-white {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: white;
    color: var(--color-primary);
    padding: 16px 36px;
    border-radius: var(--radius-sm);
    font-size: 14px;
    font-weight: 500;
    transition: var(--transition-base);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
  }

  .btn-cta-white:hover { transform: translateY(-2px); box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2); }

  .btn-cta-outline {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    border: 2px solid rgba(255, 255, 255, 0.4);
    color: white;
    padding: 14px 36px;
    border-radius: var(--radius-sm);
    font-size: 14px;
    transition: var(--transition-base);
  }

  .btn-cta-outline:hover { border-color: rgba(255, 255, 255, 0.7); background: rgba(255, 255, 255, 0.08); transform: translateY(-2px); }

  /* === Responsive === */
  @media (max-width: 768px) {
    .detail-container {
      grid-template-columns: 1fr;
      gap: 32px;
    }

    .detail-img { max-width: 280px; }
  }

  @media (max-width: 640px) {
    .page-header { padding: 88px 16px 20px; }
    .detail-insurance { padding: 20px; }
    .cta-actions { flex-direction: column; align-items: stretch; }
    .btn-cta-white, .btn-cta-outline { justify-content: center; }
  }
</style>
